services:
  # FastAPI 애플리케이션
  api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - OPENAI_API_KEY=${OPENAI_API_KEY}
        - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4.1}
        - DB_HOST=${DB_HOST}
        - DB_PORT=${DB_PORT:-3306}
        - DB_USER=${DB_USER}
        - DB_PASSWORD=${DB_PASSWORD}
        - DB_NAME=${DB_NAME}
        - QDRANT_URL=${QDRANT_URL:-}
        - QDRANT_HOST=${QDRANT_HOST:-}
        - QDRANT_PORT=${QDRANT_PORT:-6333}
        - QDRANT_API_KEY=${QDRANT_API_KEY}
        - WEB_SEARCH_API_KEY=${WEB_SEARCH_API_KEY}
        - WEB_SEARCH_ENGINE_ID=${WEB_SEARCH_ENGINE_ID}
        - LANGSMITH_API_KEY=${LANGSMITH_API_KEY}
        - LANGSMITH_PROJECT=${LANGSMITH_PROJECT:-fx-hedge-agent}
        - LANGSMITH_TRACING=${LANGSMITH_TRACING:-true}
        - API_HOST=${API_HOST:-0.0.0.0}
        - API_PORT=${API_PORT:-8000}
    container_name: fx_hedge_api
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4.1}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT:-3306}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - QDRANT_URL=${QDRANT_URL:-}
      - QDRANT_HOST=${QDRANT_HOST:-}
      - QDRANT_PORT=${QDRANT_PORT:-6333}
      - QDRANT_API_KEY=${QDRANT_API_KEY}
      - WEB_SEARCH_API_KEY=${WEB_SEARCH_API_KEY}
      - WEB_SEARCH_ENGINE_ID=${WEB_SEARCH_ENGINE_ID}
      - LANGSMITH_API_KEY=${LANGSMITH_API_KEY}
      - LANGSMITH_PROJECT=${LANGSMITH_PROJECT:-fx-hedge-agent}
      - LANGSMITH_TRACING=${LANGSMITH_TRACING:-true}
      - API_HOST=${API_HOST:-0.0.0.0}
      - API_PORT=${API_PORT:-8000}
    # depends_on:
    #   - mariadb
    #   - qdrant
    volumes:
      - ./src:/app/src
      - ./api:/app/api
      - ./config.json:/app/config.json
    networks:
      - fx_hedge_network

  # MariaDB (로컬 개발용 - 실제로는 GCP 사용)
  # mariadb:
  #   image: mariadb:latest
  #   container_name: fx_hedge_mariadb
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=${DB_ROOT_PASSWORD:-rootpassword}
  #     - MYSQL_DATABASE=${DB_NAME:-fx_hedge}
  #     - MYSQL_USER=${DB_USER:-fx_user}
  #     - MYSQL_PASSWORD=${DB_PASSWORD:-fx_password}
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mariadb_data:/var/lib/mysql
  #   networks:
  #     - fx_hedge_network
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Qdrant (로컬 개발용 - 실제로는 GCP 사용)
  # qdrant:
  #   image: qdrant/qdrant:latest
  #   container_name: fx_hedge_qdrant
  #   ports:
  #     - "6333:6333"
  #     - "6334:6334"
  #   volumes:
  #     - qdrant_data:/qdrant/storage
  #   networks:
  #     - fx_hedge_network
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:6333/health"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

networks:
  fx_hedge_network:
    driver: bridge

