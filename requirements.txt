# 외환 헷지전략 에이전트 Requirements (로컬 환경용)
# 로컬 Mi:dm 2.0 모델 및 멀티 에이전트 시스템용

# Python 버전 요구사항
# Python 3.11+ 권장

# 핵심 LLM 및 AI 라이브러리 (로컬 모델용)
transformers>=4.41.0
torch>=2.2.0
accelerate>=0.28.0
sentence-transformers>=2.7.0

# 로컬 모델 지원 (API 키 불필요)
# Mi:dm 2.0은 로컬에서 직접 실행
# GPT API 호환 클라이언트 (선택사항)
openai>=1.0.0
httpx>=0.25.0

# 비동기 처리 및 웹 요청
aiohttp>=3.8.0
asyncio-mqtt>=0.13.0

# 데이터베이스 연결 (로컬 환경)
# TSDB (Time Series Database) - 추후 개발
influxdb-client>=1.30.0
# VDB (Vector Database) - 로컬 ChromaDB
chromadb>=0.4.0
# RDB (Relational Database) - CSV 파일로 대체
# psycopg2-binary>=2.9.0  # PostgreSQL은 추후 사용
sqlalchemy>=2.0.0

# 데이터 처리 및 분석
pandas>=2.2.2
numpy>=1.24.0
scipy>=1.10.0

# 외환 데이터 수집
yfinance>=0.2.40
requests>=2.31.0
beautifulsoup4>=4.12.0

# 웹 인터페이스
streamlit>=1.36.0
plotly>=5.17.0

# 환경 설정 및 유틸리티
python-dotenv>=1.0.0
tqdm>=4.65.0
pydantic>=2.0.0

# 로깅 및 모니터링
structlog>=23.0.0

# 한국어 자연어 처리 (로컬 임베딩 모델용)
# jhgan/ko-sroberta-multitask 모델 지원
transformers[torch]>=4.35.0

# 개발 및 테스트 도구
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0

# 보안
cryptography>=41.0.0

# 성능 최적화
uvloop>=0.17.0  # Linux/macOS에서 asyncio 성능 향상

# 해시 함수 (ChromaDB 등에서 사용)
xxhash>=3.5.0